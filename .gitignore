__pycache__/
*.pyc

# Le locandine vivono nel bucket Supabase "locandine", non qui: committarle
# faceva crescere .git di ~340 MB l'anno di blob che non escono piu'. La
# cartella resta come archivio locale del downloader, ma fuori dal repo.
# Vedi assets/eventi/README.md e loc_path() in scripts/genera_eventi.py.
assets/eventi/*
!assets/eventi/README.md

# Le prove di fumo (tests/) girano con Playwright: le dipendenze si
# reinstallano da package-lock.json, non si committano.
tests/node_modules/

# Gli export di Search Console: restano sul PC, non in git.
# Sono ~1 MB a settimana (50 MB l'anno) e servono solo a rifare un conto
# vecchio. Quello che serve ai confronti e' data/gsc-storico.json, che sono
# poche righe per lettura ed e' tracciato. E' la stessa divisione delle
# locandine: il pesante fuori, il derivato leggero dentro.
gsc/
